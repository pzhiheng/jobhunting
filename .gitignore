node_modules/
dist/
.env
jobs.db
jobs.db-*

# Personal résumé — never commit (PII), used locally by curate/analyze
resume.md
resume.pdf

# Generated runtime config (personal job-search prefs) — regenerated by `configure`
filter.json
request.md

# Test / tooling artifacts
test-results/
playwright-report/

# Office temp & lock files
~$*
.~*
